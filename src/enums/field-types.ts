enum FieldTypes {
  ID = "Id",
  TEXT = "Text",
  NUMBER = "Number",
  BOOLEAN = "Boolean",
  RELATION = "Relation",
  DATE = "Date",
  JSON = "JSON",
  IDENTITY = "Identity",
}

export default FieldTypes;
